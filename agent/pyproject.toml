[project]
name = "aivideo-agent"
version = "2.0.0"
description = "AI Video Platform V2 — Desktop Agent"
requires-python = ">=3.11"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "W", "UP", "B"]

[tool.pytest.ini_options]
testpaths = ["tests"]
