"""Hằng số dùng chung — tránh magic number (SPEC 18)."""

from __future__ import annotations

DEFAULT_PAGE_LIMIT = 50
MAX_PAGE_LIMIT = 200

DEFAULT_MAX_RETRIES = 3
DEFAULT_STEP_PRIORITY = 100

DEFAULT_PIPELINE = "faceless_v1"
